# Copy this file to ".env" (same folder as package.json) and fill in your real backend URL.
# Locally, if you leave VITE_API_URL empty, it will fall back to http://localhost:5000/api

VITE_API_URL=https://your-backend-domain.com/api
